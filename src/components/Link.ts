import styled from "styled-components";

const Link = styled.a`
  color: #00bcd4;
  text-decoration: none;

  &:visited {
    color: #008899;
  }

  &:hover {
    text-decoration: underline;
  }
`;

export default Link;
